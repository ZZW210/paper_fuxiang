# Calibration Report

- no uncertainty conflicts: 57 (paper reference about 53)
- uncertainty conflicts: 106 (paper reference about 97)
- status: within broad reproduction tolerance

The counts are generated from a configurable random city, A* routes, and the sigma_t model. They are not hard-coded. Tune `conflict.sigma0`, `conflict.sigma_rate`, and the traffic pulse settings to move the default scenario closer to the paper tables if desired.
